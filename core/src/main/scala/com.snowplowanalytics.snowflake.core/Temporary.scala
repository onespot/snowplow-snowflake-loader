package com.snowplowanalytics.snowflake.core

// TODO: remove it and use "snowplow-snoflake-..." instead, when ready for public release
object Temporary {
  val TransformerName = "strawberry-transformer"
  val LoaderName = "strawberry-loader"
}
